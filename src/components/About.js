import React from 'react';

const About = () => {
    return ( 
        <div className='container'>
            <h3 className='center'>About</h3>
            <p className='center'>This project is Single Page App (SPA) that retrieves a list of users and allows you to access the user profiles.
                This page has a simple filter by name and surname. Also, a user can then book a slot using a calendar.</p>
        </div>
     );
}
 
export default About;